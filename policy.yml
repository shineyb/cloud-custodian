policies:
  - name: fsx-kms-key-filter
    description: |
      fsx must be encrypted with specific KMS key
    resource: fsx
    filters:
        - type: kms-key
          key: c7n:AliasName
          value: "^(alias/aws/fsx)"
          op: regex
